name: Starting bot ...

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@master

      - name: echo it
        run: |
          echo "ok lets start to test workflows"
      - name: List files in the repository
        run: |
          ls ${{ github.workspace }}
      - name: get ready ubuntu for run
        run: |
          echo "lets start"
          sudo apt update -y
          sudo apt upgrade -y
          echo "Ubuntu ready"
      - name: run bot
        run: |
          sudo apt install -y make nodejs python build-essential
          clear
          echo "all ready to run"
          npm install
          node server

      - run: echo "everything done!"
